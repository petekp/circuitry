{
  "schema_version": 1,
  "id": "heldout-markdown-nested-url",
  "split": "regression",
  "title": "Markdown link parser truncates URLs with balanced parentheses",
  "allowed_changed_files": [
    "src/links.mjs",
    "tests/links.test.mjs"
  ],
  "checks": [
    {
      "id": "node-test",
      "argv": ["npm", "test"]
    }
  ],
  "prompt": "Fix the Markdown link parser so URLs may contain balanced parentheses, such as `/docs/foo(bar)`, without truncating the URL. The regression command is `npm test`; it should fail before the fix and pass after the fix. Keep the change focused."
}
