{
  "schema_version": 1,
  "id": "regression-money-negative",
  "split": "regression",
  "title": "Currency parser drops negative signs and accounting parentheses",
  "allowed_changed_files": [
    "src/money.mjs",
    "tests/money.test.mjs",
    "tests/money.edge.test.mjs"
  ],
  "checks": [
    {
      "id": "node-test",
      "argv": ["npm", "test"]
    },
    {
      "id": "edge-test",
      "argv": ["npm", "run", "edge"]
    }
  ],
  "prompt": "Fix the currency amount parser so negative amounts stay negative, including standard minus signs and accounting parentheses. The regression command is `npm test`; it should fail before the fix and pass after the fix. Keep the change focused."
}
