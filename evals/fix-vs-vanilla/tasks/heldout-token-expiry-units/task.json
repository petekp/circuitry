{
  "schema_version": 1,
  "id": "heldout-token-expiry-units",
  "split": "regression",
  "title": "Token expiry helper confuses seconds and milliseconds",
  "allowed_changed_files": [
    "src/tokens.mjs",
    "tests/tokens.test.mjs",
    "tests/tokens.edge.test.mjs"
  ],
  "checks": [
    {
      "id": "node-test",
      "argv": ["npm", "test"]
    },
    {
      "id": "edge-test",
      "argv": ["npm", "run", "edge"]
    }
  ],
  "prompt": "Fix the token expiry helper. Token records may use `expiresAt` in seconds or `expiresAtMs` in milliseconds, and both formats must compare correctly against `nowMs`. The regression command is `npm test`; it should fail before the fix and pass after the fix. Keep the change focused."
}
