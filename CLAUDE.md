@AGENTS.md

<!--
Circuit keeps one canonical agent rules file: AGENTS.md.
This file exists only because Claude Code loads CLAUDE.md and does not read
AGENTS.md natively (Codex reads AGENTS.md natively). The single `@AGENTS.md`
import above pulls the same rules into Claude Code at session start, so both
hosts share one source of truth.
Do not add rules here. Edit AGENTS.md instead.
-->
