# HANDOFF

Last updated: 2026-05-06. The retained-runtime compatibility posture is superseded. There are zero external users, so the v2 work should move toward final cutover instead of preparing more external review packets. Do not continue the Phase 5.60 review-packet path unless a genuinely new ambiguity appears. Group 2 now makes retained and v1 run folders fail closed instead of adapting them.

Next group: decide whether to do doc compression now or first remove dead retained/v1 projection code that no active CLI path calls anymore. Keep the historical checkpoint docs until the doc compression group, and preserve unrelated dirty work in the repo.
