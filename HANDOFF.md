# HANDOFF

Last updated: 2026-05-06. The retained-runtime compatibility posture is superseded. There are zero external users, so the v2 work should move toward final cutover instead of preparing more external review packets. Do not continue the Phase 5.60 review-packet path unless a genuinely new ambiguity appears. Group 1 is this small policy reset only.

Next group: make retained and v1 run folders fail closed with exactly `This run folder was created by the retired runtime. Start a fresh run.` Do not add a v1 run-folder adapter. Work in numbered groups, keep the historical checkpoint docs until final doc compression, and preserve unrelated dirty work in the repo.
