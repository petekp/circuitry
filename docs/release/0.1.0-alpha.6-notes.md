# Circuit 0.1.0-alpha.6 Notes

Date: 2026-05-19

Circuit 0.1.0-alpha.6 is a plugin-only alpha for Claude Code and Codex. The
root `circuit` package remains private in this checkout; npm package publishing
is not part of this alpha.

## Shipping

- Public flow packages: Build, Explore, Fix, Pursue, and Review.
- Claude Code: plugin command surfaces, including `/circuit:run`,
  `/circuit:explore`, `/circuit:review`, `/circuit:fix`, `/circuit:build`,
  `/circuit:create`, and `/circuit:handoff`.
- Codex: bundled Circuit plugin skills and flow files. Codex host behavior is
  model-mediated until native support lands.
- CLI: `./bin/circuit run --goal "<task>"` and explicit flow invocation,
  including `./bin/circuit run pursue --goal "<task>"`.

## Fix Lite Exception

Bare `fix:` selects the Fix flow at normal depth. Lite requires an explicit
quick, small, tiny, or simple hint, or an explicit `--rigor lite` flag.

## Proof And Release Checks

The checked-in release proof set is the proof for this alpha. It covers doing
work, deciding, continuity, customization, failure, first run, and plan
execution through `docs/release/proofs/index.yaml`.

Use these release checks for this alpha:

```bash
npm run check-flow-drift
npm run check-release-ready
npm run publish:plugins:check
```

Run `npm run capture-proofs:golden-runs` only when a release diff changes
runtime control flow, flow behavior, command semantics, progress, summaries,
reports, checkpoints, or proof scenarios.

## Current Verification Record

Run from `/Users/petepetrash/Code/circuit` on 2026-05-19:

| Check | Result |
| --- | --- |
| `npm run check-flow-drift` | Passed. Generated command mirrors, flow manifests, and plugin runtime are in sync. |
| `npm run check-release-ready` | Passed. The Fix Lite behavioral gap remains tracked as an approved exception. |
| `npm run sync:codex-plugin-cache` | Passed after filesystem approval. The local Codex plugin cache was refreshed at `~/.codex/plugins/cache/circuit-local/circuit/0.1.0-alpha.6`. |
| `npm run check:codex-plugin-cache` | Passed. Source and cache both reported 26 owned files, with no missing, stale, or extra owned files. |
| `command -v circuit` | Did not find a global `circuit` launcher on PATH in this shell. Local release checks therefore used `./bin/circuit` from the repo and the bundled plugin runtime from a temp repo. |
| `node /Users/petepetrash/Code/circuit/plugins/circuit/scripts/circuit.ts doctor` from `/private/tmp/circuit-host-trial` | Passed. The Codex doctor used the bundled runtime and temp-repo smoke checks passed; hook feature visibility remains a warning. |
| `npm run smoke:host:codex` | Skipped after safe preflight. Codex CLI, Circuit launcher, and hooks feature visibility were present; live injection needs `--use-real-user-hooks`. |
| `npm run smoke:host:claude` | Skipped. Claude Code CLI and plugin validation were present, but the CLI was not authenticated for live smoke. |
| `npm run publish:plugins:check` | Passed. The generated report recorded aligned `0.1.0-alpha.6` source, Claude, Codex, and Claude marketplace versions, with no warnings or errors. |

## Non-Shipping In This Alpha

- Native Codex App Server and Claude Agent SDK adapters.
- `codex-isolated` writable worker support.
- Cross-run project-memory query and recall.
- Automatic keep-up-for-you update channel.
- Public `/circuit:pursue` slash command.
- External same-task comparison demo. Current proof is the checked-in release
  proof set, not an external comparison study.

## Host Support Wording

Describe Claude Code and Codex support as plugin command/skill surfaces with
model-mediated host affordances. Do not claim native host adapters or a polished
generic-shell human progress surface for this alpha.
