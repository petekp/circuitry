{
  "name": "circuit-next",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Claude Code plugin that automates common developer flows with per-step configurability (model, effort, skills), grounded in evidence-based prompting research and contract-first + architecture-first + tiny-step-ratcheting methodology.",
  "bin": {
    "circuit-next": "./bin/circuit-next"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "test:fast": "vitest run --exclude 'tests/runner/**'",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "soak:runtime:fast": "vitest run tests/soak tests/runner/cli-runtime.test.ts tests/runtime tests/parity",
    "soak:runtime": "vitest run tests/soak && npm run verify && npm run check-flow-drift",
    "lint": "biome check .",
    "format": "biome format --write .",
    "emit-flows": "npm run build && node scripts/emit-flows.mjs",
    "check-flow-drift": "npm run build && node scripts/emit-flows.mjs --check",
    "emit-release": "npm run build && node scripts/release/emit-current-capabilities.mjs && node scripts/release/render-parity-matrix.mjs && node scripts/release/render-readiness-report.mjs",
    "capture-proofs:golden-runs": "npm run build && node scripts/release/capture-golden-run-proofs.mjs",
    "check-release-infra": "npm run build && node scripts/release/emit-current-capabilities.mjs --check && node scripts/release/check-parity.mjs && node scripts/release/check-public-claims.mjs && node scripts/release/check-proof-coverage.mjs && node scripts/release/render-parity-matrix.mjs --check && node scripts/release/render-readiness-report.mjs --check && node scripts/release/audit-public-docs.mjs",
    "check-release-ready": "npm run check-release-infra && node scripts/release/check-release-ready.mjs",
    "smoke:host:codex": "node scripts/host-smoke/codex-handoff.mjs",
    "smoke:host:claude": "node scripts/host-smoke/claude-handoff.mjs",
    "sync:codex-plugin-cache": "node scripts/sync-codex-plugin-cache.mjs",
    "check:codex-plugin-cache": "node scripts/sync-codex-plugin-cache.mjs --check",
    "publish:plugins": "node scripts/publish-plugins.mjs check",
    "publish:plugins:bump": "node scripts/publish-plugins.mjs bump",
    "publish:plugins:check": "node scripts/publish-plugins.mjs check",
    "publish:plugins:local": "node scripts/publish-plugins.mjs local",
    "publish:plugins:release": "node scripts/publish-plugins.mjs release --codex-source petekp/circuit-next --codex-marketplace circuit-next",
    "verify": "npm run check && npm run lint && npm run build && npm run test && node scripts/emit-flows.mjs --check && npm run check-release-infra",
    "verify:fast": "npm run check && npm run lint && npm run build && npm run test:fast && node scripts/emit-flows.mjs --check",
    "circuit:run": "./bin/circuit-next"
  },
  "dependencies": {
    "yaml": "^2.8.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^22.9.0",
    "@vitest/coverage-v8": "^2.1.4",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  },
  "packageManager": "npm@11.13.0"
}
