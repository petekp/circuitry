#!/usr/bin/env node
"use strict";var __defProp=Object.defineProperty;var __name=(target,value)=>__defProp(target,"name",{value,configurable:true});var INSTALLED_SURFACE_ROOTS=[".claude-plugin","commands","hooks","schemas","scripts","skills","circuit.config.example.yaml"];var REPO_INSTALLED_SCRIPT_PATHS=["scripts/sync-to-cache.sh","scripts/verify-install.sh","scripts/relay","scripts/runtime/bin","scripts/runtime/generated"];function listInstalledSurfaceRoots(){return[...INSTALLED_SURFACE_ROOTS]}__name(listInstalledSurfaceRoots,"listInstalledSurfaceRoots");function listInstalledSurfaceSeedPaths(mode){if(mode==="installed"){return listInstalledSurfaceRoots()}return[...INSTALLED_SURFACE_ROOTS.filter(root=>root!=="scripts"),...REPO_INSTALLED_SCRIPT_PATHS]}__name(listInstalledSurfaceSeedPaths,"listInstalledSurfaceSeedPaths");function unknownOption(value,valid){return`Unknown option: ${value} (valid: ${valid.join(", ")})`}__name(unknownOption,"unknownOption");function main(){const args=process.argv.slice(2);const repoPaths=args.includes("--repo-paths");const unknownArgs=args.filter(arg=>arg!=="--repo-paths");if(unknownArgs.length>0){process.stderr.write(`${unknownOption(unknownArgs.join(", "),["--repo-paths"])}
`);return 1}const values=repoPaths?listInstalledSurfaceSeedPaths("repo"):listInstalledSurfaceRoots();process.stdout.write(`${values.join("\n")}
`);return 0}__name(main,"main");process.exit(main());
